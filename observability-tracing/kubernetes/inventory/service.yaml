apiVersion: v1
kind: Service

metadata:
  name: inventory-service
  namespace: observability-app

  labels:
    app: inventory-service

spec:
  selector:
    app: inventory-service

  ports:
  - name: http
    port: 5001
    targetPort: 5001

  type: ClusterIP
