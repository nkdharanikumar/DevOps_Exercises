apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: observability-app

  labels:
    app: payment-service

spec:
  selector:
    app: payment-service

  ports:
  - name: http
    port: 5002
    targetPort: 5002

  type: ClusterIP
